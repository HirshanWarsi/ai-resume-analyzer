{
  "name": "frontend",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./resume-analyzer run dev",
    "build": "npm --prefix ./resume-analyzer run build",
    "preview": "npm --prefix ./resume-analyzer run preview"
  }
}
